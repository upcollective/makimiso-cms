# Railway Environment Template
# Copy these to Railway environment variables

NODE_ENV=production
HOST=0.0.0.0
PORT=1337

# Generate new secure keys for production
APP_KEYS=makimiso-prod-key-1,makimiso-prod-key-2,makimiso-prod-key-3,makimiso-prod-key-4
API_TOKEN_SALT=railway-api-token-salt-production
ADMIN_JWT_SECRET=railway-admin-jwt-secret-production
TRANSFER_TOKEN_SALT=railway-transfer-token-salt-production

# Database - Railway will provide PostgreSQL
DATABASE_CLIENT=postgres
DATABASE_HOST=${PGHOST}
DATABASE_PORT=${PGPORT}
DATABASE_NAME=${PGDATABASE}
DATABASE_USERNAME=${PGUSER}
DATABASE_PASSWORD=${PGPASSWORD}
DATABASE_SSL=false

# CORS for your GitHub Pages site
CORS_ORIGIN=["https://upcollective.github.io", "https://makimiso.com", "https://www.makimiso.com"]